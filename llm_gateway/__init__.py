"""llm-gateway: a lightweight, mock-backed LLM serving gateway.

Cost-aware model routing + priority scheduling with true in-flight preemption.
No GPU, no real API keys: backends are simulated.
"""

__version__ = "0.1.0"
